# Database Configuration
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL="postgresql://user:password@localhost:5432/miltrack"

# Better Auth Configuration
# Secret key for signing tokens (generate a random string)
# You can generate one with: openssl rand -base64 32
BETTER_AUTH_SECRET="your-secret-key-here-change-in-production"

# Optional: Base URL for Better Auth (auto-detected if not set)
BETTER_AUTH_URL="http://localhost:3000"

# Next.js Configuration
# Optional: Public base URL for the application
NEXT_PUBLIC_APP_URL="http://localhost:3000"
